add the authenication using session based auth as it best for it check the reason;
also in the model add the user field.


add the user info in dummy_upload

login and logout APIs

# Tasks

Password change
manage cookie as it never expriring

make a regisration page

paginations( see paginattion later)
seacrhing

simple Jwt(api/token) for token generation, now add the expiry date and refresh_rotaton and blacklist has to be explored.


# inventory
token auth,( run the codex, which made some changes thats why have to run "python manage.py migrate" and only simple token is working now have to implement jwt also codex has changes to settings.py in default auth)
pagination()


# invoice
add product quantity check constraint in the Ui
Invoice numbering auto-generate
payment method
description

edit in the Invoice (not able to edit)


# supplier
see the supplier and product relatior, product get api


# HTML TO PDF
write there are two different sources but I want it to be same.

